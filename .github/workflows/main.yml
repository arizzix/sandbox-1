name: Main

on: [push]

jobs:
#  build:
#    uses: ./.github/workflows/build.yml

  strategy:
    matrix:
      project:
        - siag
        - ocs
        - traefik1
        - traefik2

  release:
    #needs: build
    uses: ./.github/workflows/release.yml
    with:
      project_name: ${{ matrix.project }}
      skip-dev: false
