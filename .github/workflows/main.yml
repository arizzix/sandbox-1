name: Main

on: [push]

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      project_name: build
      skip-dev: false

  release:
    needs: prep-releases
    uses: ./.github/workflows/release.yml
    with:
      project_name: build
      skip-dev: false
