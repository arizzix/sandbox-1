name: Main

on: [push]

jobs:
#  build:
#    uses: ./.github/workflows/build.yml

  strategy:
    matrix:
      include:
        - site: "prod"
          dc: "site-a"
        - site: "stag"
          dc: "site-b"
  release:
    #needs: build
    uses: ./.github/workflows/release.yml
    with:
      project_name: ${{ matrix }}
      skip-dev: false
