name: Main

on: [push]

jobs:
  build:
    uses: ./.github/workflows/build.yml

  release:
    #needs: build
    uses: ./.github/workflows/release.yml
    with:
      project_name: [siag, ocs, epo, zbx]
      skip-dev: false
