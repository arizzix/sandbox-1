name: Main

on: [push]

jobs:
#  build:
#    uses: ./.github/workflows/build.yml

  release:
    #needs: build
    strategy:
      matrix:
        include:
          - site: "prod"
            dc: "site-a"
          - site: "stag"
            dc: "site-b"
    uses: ./.github/workflows/release.yml
    with:
      project_name: ${{ matrix }}
      skip-dev: false
